% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_pda_cache.R
\name{check_pda_cache}
\alias{check_pda_cache}
\title{Compare the Local PDF Cache Against the Number of Reports FEMA Lists}
\usage{
check_pda_cache(cache_directory)
}
\arguments{
\item{cache_directory}{The folder where \code{scrape_pda_pdfs()} writes PDFs.}
}
\value{
A tibble with one row and three columns: \code{listed} (the count FEMA
shows), \code{cached} (PDF files in \code{cache_directory}), and \code{difference}
(\code{listed - cached}; positive means the cache probably has reports missing).
}
\description{
Compares the FEMA-reported number of reports
to the number of PDF files in \code{cache_directory}.
}
\details{
The two numbers are not guaranteed to match exactly even when the
cache is complete. FEMA's count is the number of entries in the listing,
while the cache holds one file per unique PDF. They can differ when two
listing entries link to the same PDF, when a report was removed from the
site but its file is still on disk, or when a download failed. Treat a
match as reassuring and a large mismatch as a reason to run \code{scrape_pda_pdfs()}.
}
\examples{
\dontrun{
check_pda_cache(cache_directory = file.path("data", "pdfs"))
}
}
