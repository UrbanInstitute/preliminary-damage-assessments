% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_preliminary_damage_assessments.R
\name{scrape_pda_pdfs}
\alias{scrape_pda_pdfs}
\title{Download Preliminary Damage Assessment (PDA) PDF Reports to Disk}
\usage{
scrape_pda_pdfs(
  cache_directory,
  pages = NULL,
  max_pages = 200,
  attempts_per_page = 5,
  delay_seconds = 2,
  quiet = FALSE
)
}
\arguments{
\item{cache_directory}{The folder where scraped PDFs are written.}

\item{pages}{Which listing pages to read, as a numeric vector. The default
\code{NULL} walks the whole listing until a page returns no links. FEMA lists newest
reports first, so  \code{pages = c(0:5)} is often sufficient and faster.}

\item{max_pages}{A guard against an unbounded walk if the listing ever stops
returning empty pages. Users should generally leave this as-is; default = 200.}

\item{attempts_per_page}{How many times to try a listing page before treating
it as a failure.}

\item{delay_seconds}{Seconds to pause between searching listing pages.
Most users should leave this as-is; shortening this delay can lead to an IP block.}

\item{quiet}{Suppress progress messages? Progress is reported by default.
Warnings are always raised, regardless of this setting.}
}
\value{
Invisibly, a tibble with one row per report found on the site,
containing \code{url}, \code{destination_file}, and \code{status} (\code{"cached"},
\code{"downloaded"}, or \code{"failed"}). Called for its side effect; PDFs are written
to \code{cache_directory}.
}
\description{
Downloads every PDA report that FEMA publishes into a local
directory so that \code{get_preliminary_damage_assessments()} has a complete and
current set of source documents to parse. Run this before regenerating the
dataset; \code{get_preliminary_damage_assessments()} parses whatever is already on
disk and never fetches anything itself.
}
\details{
Walks every page of FEMA's PDA report listing at
https://www.fema.gov/disaster/how-declared/preliminary-damage-assessments/reports
and downloads any report not already present in \code{cache_directory}.
}
\examples{
\dontrun{
## refresh the local archive, then rebuild the dataset from it
scrape_pda_pdfs(cache_directory = file.path("data", "pdfs"))
get_preliminary_damage_assessments(
  file_path = file.path("data", "pdas.csv"),
  directory_path = file.path("data", "pdfs"),
  use_cache = FALSE)
}
}
